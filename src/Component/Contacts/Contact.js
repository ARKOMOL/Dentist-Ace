import React from 'react';
import NotFound from '../../NotFound/NotFound';

const Contact = () => {
    return (
        <div>
            <NotFound/>
        </div>
    );
};

export default Contact;